This is generate migartion:
e.g. npm run migration:generate <path/folder/migration>/<NameOfMigration>

```sh
npm run migration:generate src/migration/UpdateUserIdToUUID
```

This is to run migration

```sh
npm run migration:run
```
